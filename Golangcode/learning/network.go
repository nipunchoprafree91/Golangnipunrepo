package main

import (
	// "net"
	// "log"
	"fmt"
	// "time"
	"strings"

)

// func GetOutboundIP() net.IP {
// 	conn, err := net.Dial("udp", "8.8.8.8:80")
// 	if err != nil {
// 		log.Fatal(err)
// 	}
// 	defer conn.Close()
// 	localAddr := conn.LocalAddr().(*net.UDPAddr)

// 	return localAddr.IP
// }
func main(){

	// out := GetOutboundIP()
	// fmt.Println(out.String())
	// t := time.Now()
	// fmt.Println(t.Format("2006-01-02 15:04"))

	var arr []string
	arr = [4444/tcp -> 0.0.0.0:54480 5900/tcp -> 0.0.0.0:62341 ]

	fmt.Println(arr)
	// port1 := (strings.Split(st,"->"))[0]
	// port2 := (strings.Split(port1,":"))[1]
	// fmt.Println("port2: ",port2)

}